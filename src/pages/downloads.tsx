import WebShareDownloadManager from '@app/components/WebShareDownloadManager';
import type { NextPage } from 'next';

const DownloadsPage: NextPage = () => {
  return <WebShareDownloadManager />;
};

export default DownloadsPage;
